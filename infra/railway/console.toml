# Railway config-as-code — console service
# UI: Settings → Config file path = /infra/railway/console.toml
# Root Directory = / (repo root)

[build]
builder = "DOCKERFILE"
dockerfilePath = "apps/console/Dockerfile"
watchPatterns = [
  "/apps/console/**",
  "/apps/console/Dockerfile",
  "/packages/env/**",
  "/packages/ui/**",
  "/packages/config/**",
  "/infra/railway/console.toml",
  "/package.json",
  "/bun.lock",
  "/turbo.json",
]

[deploy]
healthcheckPath = "/"
healthcheckTimeout = 180
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
drainingSeconds = 15
overlapSeconds = 20
multiRegionConfig = { "asia-southeast1-eqsg3a" = { numReplicas = 1 } }
